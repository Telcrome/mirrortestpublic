name: Sync to Gitlab

run-name: ${{ github.actor }} is learning GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu:22.04
    steps:
      - run: echo "Hello, world!"